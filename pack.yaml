name: stackstorm-monitoring
description: A monitoring system for StackStorm that sends alerts to Discord.
version: 1.0.0
actions:
  - send_to_discord
rules:
  - monitor_rule
sensors:
  - system_sensor