---
name: anomaly_detection
description: This action detects abnormal activities in the system and notifies the administrator.
enabled: true
entry_point: actions/anomaly_detection.py
runner_type: python-script
parameters:
  threshold:
    type: integer
    required: true
    default: 100
    description: Threshold for detecting anomalies.
  notification_channel:
    type: string
    required: true
    default: "discord"
    description: Channel to send notifications to.
  alert_level:
    type: string
    required: true
    description: Level of alert to be sent (e.g., info, warning, critical).
